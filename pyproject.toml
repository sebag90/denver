[project]
name = "denver"
version = "1.7.4"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pick>=2.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
