services:
  denver_{{name}}:
    container_name: denver_{{name}}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: {{version}}
    volumes:
      - ${PWD}:/app
    command: tail -f /dev/null
    stdin_open: true
